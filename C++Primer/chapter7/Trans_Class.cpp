#include <iostream>
using namespace std;

int main(void)
{
	//通过形参创建一个临时的类，然后构造初始化，再给combine调用
	item.combine("321382183219837");
	item.combine(string("328137289"));
	item.combine(Sales_data("djkslajdksl"));

	item.combine(Sales_data(null_book));
	item.combine(static_cast<Sales_data>(cin));

	return 0;
}
